## Ajax

## JSON.stringify(value , replacer? , space?)
- 将对象序列化为JSON格式字符串，便于网路传输
- replace 取舍 null原样序列化
- space 空格缩进 团队规范 可读性

## js的异步处理
- js是单线程，遇到异步任务就放到event loop中跳过往下执行，等执时机到了再从event loop中取出来回调函数来执行
- 也可以使用Promise(封装异步任务)+then
- 最建议 async/await 处理异步任务
