# useContext

## 组件通信
- 父子组件通信
 - 单向数据流
 - props 传递数据 自定义事件 
  共享数据 统一数据状态管理
- 兄弟关系
- 爷孙关系
 层层传递，如果层次深，数据传递会比较麻烦
- 陌生关系

## **组件层次**比较深
- useContext 上下文
- 提供数据上下文 最外层
Context.Provider组件
跨越层级，或者不考虑层级
连接上下文，消费数据

## useContext
在组件树里开一条隧道，后代组件直接从祖先拿数据，不用一层层传 props

## 步骤
1. createContext 创建上下文
2. Provider 组件包裹组件树
3. useContext 消费上下文数据

## 自定义hooks
- use开头函数 
- hooks目录下，属于架构
- 封装了响应式，副作用等功能，复用

## 监听鼠标移动事件，鼠标坐标显示在页面上
- 抽象
 响应式的鼠标坐标
- 能抽象就能封装
 hooks目录下 